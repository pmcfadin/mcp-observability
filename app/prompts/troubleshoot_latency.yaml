id: troubleshoot_latency
template: |
  Service {{service}} has high latency in the last {{window}}.
  Return top 3 suspected causes and a mitigation plan.
inputVariables:
  - service
  - window
metadata:
  category: troubleshooting
